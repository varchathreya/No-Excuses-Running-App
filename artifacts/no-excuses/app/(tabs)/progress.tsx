import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import { Feather } from '@expo/vector-icons';
import { Screen, Header, Pill, SectionTitle, styles } from '@/components/Screen';
import { useApp } from '@/context/AppContext';
import { useColors } from '@/hooks/useColors';
export default function Progress() { const colors = useColors(); const { completedCount, totalMiles } = useApp(); return <Screen><Header eyebrow="YOUR ADAPTATION" title="Progress" action={<Pill color={colors.accent}>WEEK 01</Pill>} /><View style={[local.readiness, { backgroundColor: colors.primary }]}><View><Text style={local.overline}>TENDON READINESS</Text><Text style={local.score}>74</Text><Text style={local.scoreLabel}>GOOD · BUILDING CAPACITY</Text></View><Feather name="shield" size={64} color="#111315" /></View><View style={local.grid}><View style={[local.metric, { backgroundColor: colors.card }]}><Text style={[local.metricValue, { color: colors.foreground }]}>{totalMiles.toFixed(1)}</Text><Text style={[styles.muted, { color: colors.mutedForeground }]}>miles logged</Text></View><View style={[local.metric, { backgroundColor: colors.card }]}><Text style={[local.metricValue, { color: colors.foreground }]}>{completedCount}</Text><Text style={[styles.muted, { color: colors.mutedForeground }]}>sessions done</Text></View></View><SectionTitle>Consistency beats intensity</SectionTitle><Text style={[styles.muted, { color: colors.mutedForeground, marginBottom: 14 }]}>Your readiness score combines completed holds, walking volume, and rest consistency.</Text><View style={local.week}>{['M','T','W','T','F','S','S'].map((day, i) => <View key={`${day}-${i}`} style={{ alignItems: 'center', gap: 7 }}><View style={[local.day, { backgroundColor: i < completedCount ? colors.accent : colors.secondary }]}>{i < completedCount && <Feather name="check" size={14} color={colors.background} />}</View><Text style={[styles.muted, { color: colors.mutedForeground }]}>{day}</Text></View>)}</View><View style={[local.insight, { borderColor: colors.border }]}><Feather name="trending-up" size={20} color={colors.accent} /><View style={{ flex: 1 }}><Text style={[local.insightTitle, { color: colors.foreground }]}>Next unlock</Text><Text style={[styles.muted, { color: colors.mutedForeground }]}>Complete two more scheduled sessions to unlock your first mileage trend.</Text></View></View></Screen> }
const local = StyleSheet.create({ readiness: { borderRadius: 24, padding: 22, flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginBottom: 14 }, overline: { color: '#111315', fontFamily: 'Inter_700Bold', fontSize: 10, letterSpacing: 1.2 }, score: { color: '#111315', fontFamily: 'Inter_700Bold', fontSize: 64, lineHeight: 70, letterSpacing: -3 }, scoreLabel: { color: '#111315', fontFamily: 'Inter_700Bold', fontSize: 10, letterSpacing: .7 }, grid: { flexDirection: 'row', gap: 10, marginBottom: 24 }, metric: { flex: 1, borderRadius: 18, padding: 16 }, metricValue: { fontFamily: 'Inter_700Bold', fontSize: 27, marginBottom: 5 }, week: { flexDirection: 'row', justifyContent: 'space-between', marginBottom: 22 }, day: { width: 32, height: 32, borderRadius: 16, alignItems: 'center', justifyContent: 'center' }, insight: { borderWidth: 1, borderRadius: 18, padding: 15, flexDirection: 'row', gap: 12, alignItems: 'center' }, insightTitle: { fontFamily: 'Inter_700Bold', fontSize: 15, marginBottom: 3 } });